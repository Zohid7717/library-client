import { createAsyncThunk, createSlice } from "@reduxjs/toolkit"
import axios from "../../axios/axios"



export const getAuth = createAsyncThunk<>(
  'user/getAuth',
  async (user_name, user_pass) => {
    const 
  }
)